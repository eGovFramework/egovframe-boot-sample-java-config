apiVersion: v1
kind: Service
metadata:
  name: egovframe-boot-sample
spec:
  selector:
    app: egovframe-boot-sample
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30060
